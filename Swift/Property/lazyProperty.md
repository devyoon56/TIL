# 지연 저장 속성(Lazy Stored Property)

지연 저장 속성은 인스턴스를 초기화할 때 반드시 값을 초기화할 필요가 없는 경우 사용하는 속성입니다. 지연 저장 속성은 저장 속성의 초기화를 지연시키는 것으로 이해하면 됩니다. 지연 저장 속성은 **lazy** 키워드를 사용해서 선언합니다.

## 지연 저장 속성 선언

```swift

```
